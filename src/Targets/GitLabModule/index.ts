import YAML from 'yaml';
import SemanticModel from './../../Common/SemanticModel';

const generateConfig = (semanticModel: SemanticModel) => {
  console.log("Running from GitLab Module!")
}

export default generateConfig;